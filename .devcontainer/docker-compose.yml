version: '3'
services:
  megadraft-file-upload:
    build: .
    container_name: megadraft-file-upload
    tty: true
    ports:
      - "8080:8080"
    volumes:
      - ../:/src
    working_dir: /src
